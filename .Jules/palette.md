## 2026-05-21 - Keyboard focus and accessibility in forms
**Learning:** Chaining TextInput fields using refs and returnKeyType significantly improves the 'perceived speed' of data entry on mobile. Combining this with accessibility labels for icon-only buttons ensures the UI is both fast and inclusive.
**Action:** Always check for icon-only buttons and implement ref-based focus chaining in any new form modal.
