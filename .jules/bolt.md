## 2026-04-24 - Optimizing Dashboard Rendering
**Learning:** Memoizing complex SVG charts and stabilizing `FlatList` `renderItem` with `useCallback` effectively prevents unnecessary re-renders in React Native.
**Action:** Always look for inline functions in `FlatList` props and complex local components in screens as easy performance wins.
