## 2025-05-31 - Enhancing Form UX with Keyboard Navigation and Validation
**Learning:** Combining `autoFocus`, `returnKeyType`, and `onSubmitEditing` significantly reduces input friction in mobile-first forms by allowing hands-free field transitions.
**Action:** Always implement `useRef` for input chaining and provide visual feedback (like opacity) for disabled submit states to guide the user.
